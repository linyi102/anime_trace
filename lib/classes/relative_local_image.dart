class RelativeLocalImage {
  int imageId;
  String path;

  RelativeLocalImage(this.imageId, this.path);

  @override
  String toString() {
    return "RelativeLocalImage[imageId=$imageId, path=$path]";
  }
}
